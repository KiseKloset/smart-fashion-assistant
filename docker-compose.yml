services:
  tryon-api:
    build: tryon_api/
    restart: unless-stopped
    ports:
      - 8080:8080

  retrieval-api:
    build: retrieval_api/
    restart: unless-stopped
    ports:
      - 8081:8081